# Changelog - 2026-08-17

## Summary of the Session
1. **Corrección del Menú Lateral (Sidebar) y Header en la Página de Inicio**:
   - Corrección del fallo de detección del estado activo en el Sidebar y Header que provocaba que la categoría "Hogar" apareciera marcada en rojo al estar en la página principal (`/`).
   - Ajuste estricto de la evaluación de `request.page_type`: en Inicio (`request.page_type == 'index'`), únicamente el botón **Inicio** se resalta como activo.
   - Eliminación del parámetro `forloop.first` en el Header que forzaba el resalte en el primer enlace.
   - Agregado el enlace "Inicio" en las listas de respaldo de navegación lateral y superior.

2. **Banner Hero Principal con Soporte para Imagen de Fondo**:
   - Implementada la capacidad de cargar una **Imagen de Fondo (Desktop)** y una **Imagen de Fondo (Móvil - Opcional)** para el Banner Hero Principal en `sections/llamarada-hero-banner.liquid` y `sections/llamarada-home.liquid`.
   - Agregado control desplegable/slider de **Oscurecimiento de imagen (Overlay opacity %)** de 0% a 90% para garantizar una legibilidad óptima sobre cualquier imagen subida.
   - Renderización adaptable y de alto rendimiento mediante el tag HTML `<picture>` y los filtros `image_url` de Shopify.
   - Mantenido el degradado magenta/rojo característico como respaldo automático cuando no hay imagen seleccionada.

3. **Despliegue Directo y Limpieza de Sintaxis Liquid en la Tienda**:
   - Corrección de sintaxis Liquid (eliminación de paréntesis en condiciones `if`) en `sections/llamarada-header.liquid` y `sections/llamarada-sidebar-nav.liquid`.
   - Sincronización y despliegue en vivo exitoso directamente a la tienda Shopify mediante Shopify CLI (`shopify theme push`).
5. **Habilitación de Colores Inline del Personalizador de Shopify y Corrección de Z-Index de Overlay**:
   - Eliminación de declaraciones `!important` en `assets/llamarada-theme.css` para `.ll-hero__title`, `.ll-hero__subtitle` y `.ll-hero__cta` que bloqueaban los colores elegidos en el Personalizador de Shopify.
   - Elevación de la capa de contenido `.ll-hero__content` a `z-index: 5 !important` y capa overlay a `z-index: 2` con color de oscurecimiento configurable (`overlay_color`).

## Detailed Changes

### Componentes y Secciones
- **`sections/llamarada-sidebar-nav.liquid`** `[MODIFY]`: Ajuste de la lógica `is_link_active` e `is_block_active` para aislar el estado activo únicamente a Inicio cuando se navega en la portada. Eliminación de sintaxis no válida con paréntesis.
- **`snippets/llamarada-sidebar.liquid`** `[MODIFY]`: Agregado el enlace directo de Inicio y validación estricta del handle de colección para la categoría Hogar.
- **`sections/llamarada-header.liquid`** `[MODIFY]`: Eliminado `forloop.first` en la plantilla de navegación superior y corregida la sintaxis de condiciones `if`.
- **`sections/llamarada-hero-banner.liquid`** `[MODIFY]`: Agregado soporte HTML `<picture>`, campos en esquema JSON (`image`, `image_mobile`, `overlay_color`, `overlay_opacity`) y capa overlay.
- **`sections/llamarada-home.liquid`** `[MODIFY]`: Agregado soporte HTML y campos en el esquema JSON para imagen de fondo del Hero Banner interno y capa overlay.
- **`assets/llamarada-theme.css`** `[MODIFY]`: Eliminados estilos `!important` en colores de títulos/subtítulos/botones del Hero e incrementado `z-index: 5` para el contenido.
- **`assets/hero-banner.jpg`** `[NEW]`: Imagen de alta resolución generada para el banner principal.

6. **Implementación de Auditoría de Animaciones y Tipografía Óptica (Emil Kowalski & Apple Design)**:
   - Agregados tokens de curva de aceleración `var(--ll-ease-out)` (`cubic-bezier(0.23, 1, 0.32, 1)`) y `var(--ll-ease-spring)` (`cubic-bezier(0.16, 1, 0.3, 1)`).
   - Implementadas respuestas táctiles instantáneas `:active { transform: scale(0.97); }` (100ms ease-out) para tarjetas de productos, tarjetas de categorías, botones de agregar caja/lote y botones de acción.
   - Agregado efecto zoom sutil en imágenes de producto (`transform: scale(1.06)`) al hacer hover sobre tarjetas.
   - Aplicado *staggering* (retraso en cascada) de 40ms para animar suavemente elementos de cuadrícula.
   - Ajuste de espaciado óptico de letras (*tracking*): `-0.03em` para títulos principales e `-0.005em` para texto de cuerpo.
   - Añadida regla global de protección de movimiento reducido (`@media (prefers-reduced-motion: reduce)`).

## Detailed Changes

### Componentes y Secciones
- **`sections/llamarada-sidebar-nav.liquid`** `[MODIFY]`: Ajuste de la lógica `is_link_active` e `is_block_active` para aislar el estado activo únicamente a Inicio cuando se navega en la portada. Eliminación de sintaxis no válida con paréntesis.
- **`snippets/llamarada-sidebar.liquid`** `[MODIFY]`: Agregado el enlace directo de Inicio y validación estricta del handle de colección para la categoría Hogar.
- **`sections/llamarada-header.liquid`** `[MODIFY]`: Eliminado `forloop.first` en la plantilla de navegación superior y corregida la sintaxis de condiciones `if`.
- **`sections/llamarada-hero-banner.liquid`** `[MODIFY]`: Agregado soporte HTML `<picture>`, campos en esquema JSON (`image`, `image_mobile`, `overlay_color`, `overlay_opacity`) y capa overlay.
- **`sections/llamarada-home.liquid`** `[MODIFY]`: Agregado soporte HTML y campos en el esquema JSON para imagen de fondo del Hero Banner interno y capa overlay.
- **`assets/llamarada-theme.css`** `[MODIFY]`: Definidos tokens de animación `:root`, respuestas táctiles `:active`, zoom de imágenes, *tracking* tipográfico óptico y soporte de movimiento reducido.
- **`assets/hero-banner.jpg`** `[NEW]`: Imagen de alta resolución generada para el banner principal.

7. **Pills de Filtrado Rápido y Micro-Contraste en Badges Mayoristas (UX/UI Taste-Skill)**:
   - Integrada barra de píldoras de filtrado rápido (`.ll-deals-pills-bar` y `.ll-deal-pill`) para Ofertas de Mayoreo (*"Todos"*, *"Cajas Cerradas"*, *"Lotes Pequeños"*, *"Más Vendidos"*) con animaciones de estado activo (`scale(0.95)`).
   - Refinados los badges mayoristas (`.ll-badge`) incrementando el radio a `border-radius: 4px`, agregando borde translúcido (`border: 1px solid rgba(255,255,255,0.35)`), efecto de cristal `backdrop-filter: blur(4px)` y sombra física.

## Detailed Changes

### Componentes y Secciones
- **`sections/llamarada-home.liquid`** `[MODIFY]`: Agregada barra de píldoras de filtrado rápido `.ll-deals-pills-bar` sobre la cuadrícula de ofertas de mayoreo.
- **`sections/llamarada-wholesale-deals.liquid`** `[MODIFY]`: Agregada barra de píldoras de filtrado rápido `.ll-deals-pills-bar`.
- **`assets/llamarada-theme.css`** `[MODIFY]`: Definidas reglas `.ll-deals-pills-bar`, `.ll-deal-pill` con animación activa y textura de micro-contraste en `.ll-badge`.

8. **Remoción de Píldoras de Filtro y Forzado `!important` en Estilos Inline del Hero**:
   - Eliminada la barra de píldoras de filtrado de las secciones de ofertas de mayoreo (`sections/llamarada-home.liquid` y `sections/llamarada-wholesale-deals.liquid`) a solicitud del usuario.
   - Eliminados bloques CSS duplicados en `assets/llamarada-theme.css` que contenían `color: #ffffff !important` y sobrescribían las selecciones del editor de temas.
   - Aplicado `!important` a las reglas inline de Liquid para `overlay_color`, `overlay_opacity`, `title_color`, `subtitle_color`, `cta_bg_color` y `cta_text_color`, garantizando la edición en tiempo real desde el personalizador de Shopify.

9. **Posicionamiento Absoluto del Tag `<picture>` y Altura Estable del Hero**:
   - Corrección del tag `<picture class="ll-hero__picture">` y `.ll-hero__bg-img` con `position: absolute !important; inset: 0; width: 100%; height: 100%; object-fit: cover;` para evitar que la imagen empuje el contenido verticalmente y altere el alto del banner.
   - Definida altura mínima consistente (`min-height: 280px`) y capa `.ll-hero__overlay` con `position: absolute; z-index: 2`, manteniendo los títulos, subtítulos y botones siempre visibles y centrados en `z-index: 5`.

## Commit History
- `fix: resolve sidebar active state on home and add image support for hero banner`
- `fix: resolve liquid syntax error in header and sidebar nav`
- `feat: make hero image overlay use customizable gradient and opacity slider`
- `fix: remove hardcoded important colors and enable overlay color selector`
- `style: apply high-craft ui animations and optical typography based on audit`
- `feat: add wholesale deals filter pills and enhance badge micro-contrast`
- `fix: remove filter pills and enforce inline style important for hero customizer colors and overlay`
- `fix: make hero background image and overlay use absolute positioning with fixed min-height`
