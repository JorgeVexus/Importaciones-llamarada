# Changelog - 2026-08-20

## Summary of the Session
1. **Restauración Total del Tema Llamarada y Reversión de Importación Conflictiva**:
   - Diagnóstico profundo de la pérdida de estilos y páginas en blanco en el editor de Shopify y tienda online.
   - Detección de una importación masiva accidental de un tema externo ajeno (Arsenal / Next Theme) ocurrida el miércoles 19 de agosto, la cual introdujo más de 50 archivos incompatibles y sobrescribió `layout/theme.liquid`, las plantillas de colecciones, productos, páginas y formularios.
   - Reversión completa de la base de código al estado 100% funcional y probado del lunes 17 de agosto (commit `ddbd0f0`), eliminando de raíz todos los archivos residuales del tema externo.
   - Restauración íntegra de la cascada de estilos globales `assets/llamarada-theme.css`, el Header con buscador y logo corporativo, el Sidebar interactivo, las páginas de Colecciones, Productos, Ofertas de Mayoreo, Novedades y Contacto.

2. **Corrección de Validación en `templates/product.json`**:
   - El sync de GitHub→Shopify rechazaba `product.json` por campo `text` sin tag `<p>` envolvente (requerido por `inline_richtext`) y un bloque `disclosures` a nivel de sección que no existía en el schema.
   - Corregido el texto del vendor con `<p>{{ product.vendor }}</p>` y eliminado el bloque inválido de `disclosures`.

3. **Eliminación de Bloque Duplicado en CSS, Remoción de Clase Gradient y Ajuste del Hero CTA**:
   - Eliminación de 195 líneas duplicadas en `assets/llamarada-theme.css` que rompían la cascada de estilos de la barra lateral y el canvas principal.
   - Remoción de la clase `gradient` del elemento `<body>` en `layout/theme.liquid` para prevenir que el fondo fijo púrpura de Dawn cubra todo el viewport.
   - Aplicación de dimensiones y contención estricta al botón CTA `.ll-hero__cta` y al contenedor `.ll-hero`.

7. **Ajuste de Filas de Cuadrícula (Row Spanning) para Eliminar Separación**:
   - Corrección en `.shopify-section.section-llamarada-sidebar` configurando `grid-row: 1 / span 20` con `height: fit-content` y `align-self: start`.
   - Esto permite que la fila 1 ajuste su altura exclusivamente a la altura del Hero Banner, logrando que la sección de *Categorías Populares* inicie de inmediato debajo del banner sin espacios vacíos.

## Detailed Changes

### Reversión y Limpieza de Archivos
- **Eliminación de archivos del tema externo**: Removidos todos los componentes `sections/arsenal-*`, `snippets/` y plantillas `.storytelling` / `.immersive`.
- **Restauración de Plantillas**:
  - `layout/theme.liquid`: Restaurado con enlace directo a `llamarada-theme.css`, `base.css`, Google Fonts y remoción de clase gradient fija.
  - `sections/header.liquid` y `sections/header-group.json`: Header blanco con iconos y navegación morada.
  - `templates/collection.json`, `templates/search.json`, `templates/page.contact.json`, `templates/index.json`: Restauradas a sus secciones nativas de La Llamarada.
  - `assets/llamarada-theme.css`: Header blanco, botones verdes, eliminación de espacio scroll al pie, espaciado continuo entre hero y categorías y optimización de grid.
  - `sections/llamarada-hero-banner.liquid`: Gradiente base permanente y contención vertical en botón CTA.
- **`templates/product.json`** `[MODIFY]`: Ajuste de formato de texto en setting de vendor.

## Commit History
- `fix: restore complete llamarada theme and revert foreign arsenal theme import`
- `fix: wrap vendor text in p tag and remove invalid disclosures block from product template`
- `chore: trigger github to shopify webhook sync`
- `fix: remove duplicate css block remove body gradient and constrain hero cta`
- `fix: adjust vendor text setting in product template`
- `fix: correct 2-column grid layout specificity for sidebar and canvas`
- `style: update header to white with purple links and set green theme buttons`
- `fix: adjust sidebar row span to eliminate gap between hero and categories`
